# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ruuvitag-ios' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ruuvitag-ios
  pod 'SwiftBytes', :git => 'https://github.com/Tombio/swift-bytes.git'

  target 'ruuvitag-iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
